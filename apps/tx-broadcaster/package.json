{
  "name": "tx-broadcaster",
  "version": "1.0.0",
  "description": "Transaction broadcaster service for asset withdrawal system",
  "main": "src/main.ts",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "build": "cd ../.. && pnpm nx run tx-broadcaster:build",
    "test": "cd ../.. && pnpm nx run tx-broadcaster:test",
    "lint": "cd ../.. && pnpm nx run tx-broadcaster:lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.18.2",
    "tsx": "^3.0.0",
    "ioredis": "^5.3.2",
    "@aws-sdk/client-sqs": "^3.450.0",
    "ethers": "^6.8.0",
    "database": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
