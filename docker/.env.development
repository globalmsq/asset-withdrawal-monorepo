# Development environment variables for docker-compose
# WARNING: These are development-only values. Use secure values in production.

# MySQL Configuration
MYSQL_ROOT_PASSWORD=development-password-change-in-production

# LocalStack Configuration
# For development, we allow localhost origins. In production, specify your actual domains.
LOCALSTACK_CORS_ORIGINS=http://localhost:*,http://127.0.0.1:*