# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build tx-broadcaster`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tx-broadcaster`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tx-broadcaster && \
          adduser --system -G tx-broadcaster tx-broadcaster

COPY dist/apps/tx-broadcaster tx-broadcaster/
COPY apps/tx-broadcaster/package.json tx-broadcaster/
RUN chown -R tx-broadcaster:tx-broadcaster .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tx-broadcaster --omit=dev -f install

CMD [ "node", "tx-broadcaster" ]
